import React from 'react';

export const LotusBackground = () => (
    <div className="fixed inset-0 z-0 flex items-center justify-center overflow-hidden pointer-events-none" style={{ opacity: 'var(--petal-opacity, 0.04)' }}>
        {/* Removed rotating background pattern as per user request */}
    </div>
);
